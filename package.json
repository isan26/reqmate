{
  "name": "courier",
  "version": "1.0.0",
  "description": "Lightway dependency free Http client on top of fetch for browser, NodeJS, Bun etc.., include retry, polling and caching",
  "keywords": [
    "http",
    "client",
    "fetch",
    "retry",
    "polling",
    "caching",
    "browser",
    "nodejs",
    "bun"
  ],
  "main": "dist/index.js",
  "scripts": {
    "test": "rm -rf ./dist &&  jest",
    "build": "tsc"
  },
  "engines": {
    "node": ">=18.0.0",
    "bun": ">=1.0.0"
  },
  "author": "Isan",
  "license": "ISC",
  "devDependencies": {
    "@types/jest": "^29.5.4",
    "jest": "^29.6.4",
    "ts-jest": "^29.1.1",
    "typescript": "^5.2.2"
  },
  "jest": {
    "preset": "ts-jest",
    "testEnvironment": "node"
  }
}
