{
  "name": "courier",
  "version": "1.0.0",
  "description": "Http client on top of fetch for browser and NodeJS, include retry, polling and caching",
  "main": "dist/index.js",
  "scripts": {
    "test": "rm -rf ./dist &&  jest"
  },
  "author": "Isan",
  "license": "ISC",
  "devDependencies": {
    "@types/jest": "^29.5.4",
    "jest": "^29.6.4",
    "ts-jest": "^29.1.1",
    "typescript": "^5.2.2"
  },
  "jest": {
    "preset": "ts-jest",
    "testEnvironment": "node"
  }
}
